{
  "name": "qce-studio",
  "private": true,
  "version": "0.1.0",
  "description": "QCE Studio - Knowledge Engineering Platform",
  "packageManager": "pnpm@10",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.0.0"
  }
}
