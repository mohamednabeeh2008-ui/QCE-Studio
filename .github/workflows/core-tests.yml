name: Core Tests

on:
  pull_request:
    paths:
      - "packages/core/**"
      - "package.json"
      - "pnpm-workspace.yaml"
      - ".github/workflows/core-tests.yml"
  push:
    branches:
      - main
    paths:
      - "packages/core/**"
      - "package.json"
      - "pnpm-workspace.yaml"
      - ".github/workflows/core-tests.yml"

permissions:
  contents: read

jobs:
  core-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Run core tests
        run: pnpm --filter @qce/core test:run
